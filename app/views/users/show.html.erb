<div class="container">
    <div class="row">
        <div class="col-lg-12 d-flex align-items-center">
            <div class="card" style="width:100%;margin-top:5%;min-height:80vh;">
                <div class="card-body">
                    <h3 class="card-title"><%= @user.first_name %> <%= @user.last_name %> || <%= @user.role.upcase %></h3>
                    <hr>
                </div>
            </div>
        </div>
    </div>
</div>
